{
  "title": "Patent Lookup MCP",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "tool": {
      "title": "Tool Name",
      "type": "string",
      "description": "MCP tool to call",
      "enum": ["get_patent_details", "find_patent_citations", "search_patents", "patent_landscape_by_company"],
      "editor": "select",
      "prefill": "get_patent_details"
    },
    "params": {
      "title": "Parameters (JSON)",
      "type": "object",
      "description": "Tool parameters as JSON object",
      "editor": "json",
      "prefill": {"patent_number": "US10123456"}
    }
  }
}